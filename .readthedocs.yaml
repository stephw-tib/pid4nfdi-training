version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

clone:
    depth: full
    
python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py
  
